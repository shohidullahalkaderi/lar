apiVersion: v1
kind: Secret
metadata:
  name: laravel-secrets
  labels:
    app.kubernetes.io/name: laravel-app
type: Opaque
stringData:
  # [SECURITY]: Core application secret key and database/service credentials secured via Kubernetes Secret.
  DB_PASSWORD: "laravel_password"
  MYSQL_ROOT_PASSWORD: "root_password"
  REDIS_PASSWORD: "password_secure"

---
# [PERSISTENCE]: Standalone Persistent Volume Claim for media uploads (bypasses hostPath / VS Code sync).
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: laravel-media-pvc
  labels:
    io.kompose.service: app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: v1
kind: Service
metadata:
  name: app
  labels:
    io.kompose.service: app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    io.kompose.service: app

---
apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    io.kompose.service: db
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
  selector:
    io.kompose.service: db

---
apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    io.kompose.service: redis
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  selector:
    io.kompose.service: redis

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  labels:
    io.kompose.service: app
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: app
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.service: app
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        # [SECURITY HARDENING]: Enforce RuntimeDefault seccomp profile to restrict kernel syscalls and prevent privilege escalation / kernel exploits.
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: laravel-app
          image: laravel-app:dev
          # [MODIFIED]: Always -> IfNotPresent. Reverted for local kind/Docker Desktop dev:
          # this image is built locally and never pushed to a registry, so imagePullPolicy: Always 
          # caused ErrImagePull / ImagePullBackOff. IfNotPresent is correct here since the cluster 
          # uses the image already loaded into its local store. Switch to Always + an immutable tag 
          # (e.g. git SHA) pushed to a real registry once this moves to an actual prod cluster.
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            # [SECURITY HARDENING]: Make root filesystem read-only to prevent unauthorized code modifications or binary tampering and prevent malware from writing scripts or modifying system binaries on disk.
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 8000
              protocol: TCP
          env:
            - name: XDG_CONFIG_HOME
              value: "/tmp"
            - name: XDG_DATA_HOME
              value: "/tmp"
            - name: OCTANE_STATE_FILE
              value: "/tmp/octane-state.json"
            - name: APP_KEY
              value: "base64:XphRDMjYzEP4KKvs8rej6y4Y68wRaHvlA6czHrEOqsM="
            - name: DB_HOST
              value: "db"
            - name: DB_DATABASE
              value: "laravel_db"
            - name: DB_USERNAME
              value: "laravel_user"
            - name: DB_PORT
              value: "3306"
            - name: REDIS_CLIENT
              value: "phpredis"
            - name: REDIS_HOST
              value: "redis"
            - name: REDIS_PORT
              value: "6379"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: laravel-secrets
                  key: DB_PASSWORD
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: laravel-secrets
                  key: REDIS_PASSWORD
          volumeMounts:
            # [SECURITY HARDENING]: Writable ephemeral storage required for runtime temp files when root filesystem is read-only.
            - mountPath: /tmp
              name: app-tmp
            # [SECURITY HARDENING]: Writable ephemeral storage required for Laravel framework bootstrap cache files.
            - mountPath: /usr/src/app/bootstrap/cache
              name: laravel-cache
            # [SECURITY HARDENING]: Writable ephemeral storage for framework states and logs.
            - mountPath: /usr/src/app/storage/framework
              name: laravel-framework
            - mountPath: /usr/src/app/storage/logs
              name: laravel-logs
            # [PERSISTENCE]: Persistent volume mount for media uploads so they survive pod restarts.
            - mountPath: /usr/src/app/storage/app/public
              name: laravel-media
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 10
          readinessProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 8000
            # [30-second SSE streaming setup]: Set to 35s. Since streams recycle every 30 seconds, 
            # we give it a tiny buffer past 30s before the liveness check starts.
            initialDelaySeconds: 35
            # [30-second SSE streaming setup]: 15-second intervals give worker threads enough breathing room 
            # during heavy concurrent stream requests.
            periodSeconds: 15
            timeoutSeconds: 5
            # [30-second SSE streaming setup]: A threshold of 4 means Kubelet waits 4 consecutive failures 
            # (15s * 4 = 60 seconds of unresponsiveness) before killing the pod.
            failureThreshold: 4
      restartPolicy: Always
      volumes:
        # [SECURITY HARDENING]: Ephemeral emptyDir volume for writable /tmp directory.
        - name: app-tmp
          emptyDir: {}
        # [SECURITY HARDENING]: Ephemeral emptyDir volume for Laravel bootstrap cache folder.
        - name: laravel-cache
          emptyDir: {}
        # [SECURITY HARDENING]: Ephemeral emptyDir volume for Laravel framework storage.
        - name: laravel-framework
          emptyDir: {}
        # [SECURITY HARDENING]: Ephemeral emptyDir volume for Laravel logs storage.
        - name: laravel-logs
          emptyDir: {}
        # [PERSISTENCE]: Persistent Volume Claim backing user media uploads.
        - name: laravel-media
          persistentVolumeClaim:
            claimName: laravel-media-pvc

---
# [STATEFULSET MIGRATION]: Converted MySQL deployment to a StatefulSet for stable identities and unique per-pod storage.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: db
  labels:
    io.kompose.service: db
spec:
  serviceName: "db"
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: db
  template:
    metadata:
      labels:
        io.kompose.service: db
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        # [SECURITY HARDENING]: Enforce RuntimeDefault seccomp profile to restrict kernel syscalls.
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: laravel-mysql-db
          image: mysql:8.0
          securityContext:
            allowPrivilegeEscalation: false
            # [SECURITY HARDENING]: Make root filesystem read-only to satisfy KSV-0014 and lock down binaries and prevent malware from writing scripts or modifying system binaries on disk.
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          env:
            - name: MYSQL_DATABASE
              value: "laravel_db"
            - name: MYSQL_USER
              value: "laravel_user"
            - name: MYSQL_ROOT_HOST
              value: "127.0.0.1"
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: laravel-secrets
                  key: DB_PASSWORD
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: laravel-secrets
                  key: MYSQL_ROOT_PASSWORD
          ports:
            - containerPort: 3306
              protocol: TCP
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            exec:
              # [NOTE]: Invoked sh -c to expand $MYSQL_ROOT_PASSWORD properly during liveness checks.
              command:
                - sh
                - -c
                - mysqladmin ping -h 127.0.0.1 -uroot -p"$MYSQL_ROOT_PASSWORD"
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          volumeMounts:
            # [PERSISTENCE]: Mount persistent volume for MySQL data files to ensure data safety across restarts.
            - mountPath: /var/lib/mysql
              name: laravel-db-data
            # [SECURITY HARDENING]: Ephemeral writable volume required for MySQL runtime sockets when root filesystem is read-only.
            - mountPath: /var/run/mysqld
              name: mysql-run
            # [SECURITY HARDENING]: Ephemeral writable volume required for MySQL temp files when root filesystem is read-only.
            - mountPath: /tmp
              name: mysql-tmp
      restartPolicy: Always
      volumes:
        - name: mysql-run
          emptyDir: {}
        - name: mysql-tmp
          emptyDir: {}
  # [STATEFULSET MIGRATION]: Replaced standalone PVC with volumeClaimTemplates so each replica gets an isolated disk.
  volumeClaimTemplates:
    - metadata:
        name: laravel-db-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi

---
# [STATEFULSET MIGRATION]: Converted Redis deployment to a StatefulSet.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  labels:
    io.kompose.service: redis
spec:
  serviceName: "redis"
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: redis
  template:
    metadata:
      labels:
        io.kompose.service: redis
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        # [SECURITY HARDENING]: Enforce RuntimeDefault seccomp profile to restrict kernel syscalls.
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: laravel-redis
          image: redis:7.2-alpine
          securityContext:
            allowPrivilegeEscalation: false
            # [SECURITY HARDENING]: Make root filesystem read-only to satisfy KSV-0014 and lock down binaries and prevent malware from writing scripts or modifying system binaries on disk.
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          args:
            - redis-server
            - --requirepass
            - $(REDIS_PASSWORD)
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: laravel-secrets
                  key: REDIS_PASSWORD
          ports:
            - containerPort: 6379
              protocol: TCP
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - redis-cli -a "$REDIS_PASSWORD" ping | grep PONG
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 5
          volumeMounts:
            # [PERSISTENCE]: Mount persistent volume for Redis data storage.
            - mountPath: /data
              name: redis-data
            # [SECURITY HARDENING]: Ephemeral writable volume required for Redis temporary runtime files when root filesystem is read-only.
            - mountPath: /tmp
              name: redis-tmp
      restartPolicy: Always
      volumes:
        - name: redis-tmp
          emptyDir: {}
  # [STATEFULSET MIGRATION]: Dynamic volume template for Redis.
  volumeClaimTemplates:
    - metadata:
        name: redis-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 500Mi

---
# [SECURITY HARDENING / KIND NETWORK POLICY]: NetworkPolicy for Laravel App.
# Optimized for local kind clusters: allows open port 8000 ingress for easy debugging/port-forwarding 
# without requiring an external Nginx controller, while strictly restricting outbound traffic to 
# MySQL, Redis, and CoreDNS in the kube-system namespace.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: app-network-policy
  labels:
    app.kubernetes.io/name: laravel-app
spec:
  podSelector:
    matchLabels:
      io.kompose.service: app
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        - protocol: TCP
          port: 8000
  egress:
    - to:
        - podSelector:
            matchLabels:
              io.kompose.service: db
      ports:
        - protocol: TCP
          port: 3306
    - to:
        - podSelector:
            matchLabels:
              io.kompose.service: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53

---
# [SECURITY HARDENING / NETWORK POLICY]: NetworkPolicy for Database - 
# Strictly isolated to accept inbound traffic ONLY from the Laravel app pod.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: db-network-policy
  labels:
    io.kompose.service: db
spec:
  podSelector:
    matchLabels:
      io.kompose.service: db
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.service: app
      ports:
        - protocol: TCP
          port: 3306

---
# [SECURITY HARDENING / NETWORK POLICY]: NetworkPolicy for Redis - 
# Strictly isolated to accept inbound traffic ONLY from the Laravel app pod.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: redis-network-policy
  labels:
    io.kompose.service: redis
spec:
  podSelector:
    matchLabels:
      io.kompose.service: redis
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.service: app
      ports:
        - protocol: TCP
          port: 6379